Client ID: 5fe79f43-3c2d-4cf6-9184-478e247504b7
Client secret: uYQ6E6NvUDucLx0yRqz4zrUyeuWcX09U
# check_credentials.py
from sentinelhub import SHConfig

# 🔹 ใส่ค่า Client ID, Client Secret, Instance ID ของคุณ
CLIENT_ID = "5fe79f43-3c2d-4cf6-9184-478e247504b7"
CLIENT_SECRET = "uYQ6E6NvUDucLx0yRqz4zrUyeuWcX09U"
INSTANCE_ID = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxx"  # เปลี่ยนเป็นของจริง

# 🔹 ตั้งค่า SHConfig
config = SHConfig()
config.sh_client_id = CLIENT_ID
config.sh_client_secret = CLIENT_SECRET
config.instance_id = INSTANCE_ID

# 🔹 ตรวจสอบ validity
print("🔹 ตรวจสอบ Sentinel Hub credentials & instance")
print("Client ID:", config.sh_client_id)
print("Instance ID:", config.instance_id)
print("Is config valid?", config.is_valid)

# 🔹 ถ้า is_valid = False → credential/instance ผิด
if not config.is_valid:
    print("❌ Credential หรือ Instance ID ไม่ถูกต้อง")
    print("ลองสร้าง OAuth client ใหม่ใน Dashboard และตรวจสอบ Instance ID อีกครั้ง")
else:
    print("✅ Credential & Instance ถูกต้อง สามารถใช้ดึงข้อมูล Sentinel Hub ได้")
