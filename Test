import pandas as pd
import pickle
from sklearn.neural_network import MLPRegressor
from sklearn.preprocessing import StandardScaler

# -------------------------------------------------
# 1. โหลดข้อมูลจาก CSV
# -------------------------------------------------
# dataset.csv ต้องมีคอลัมน์: Blue, Green, Red, NIR (หรือชื่อ band ที่คุณใช้จริง)
# และคอลัมน์ target เช่น "Field_Reflectance" (ค่าการสะท้อนแสงภาคสนาม)
# -------------------------------------------------

df = pd.read_csv("dataset.csv")

# Features และ Target
X = df[["Blue", "Green", "Red", "NIR"]].values   # 👈 ปรับชื่อตาม dataset จริง
y = df["Field_Reflectance"].values               # 👈 ปรับชื่อตาม dataset จริง

# -------------------------------------------------
# 2. เตรียม Scaler
# -------------------------------------------------
scaler_X = StandardScaler()
scaler_y = StandardScaler()

X_scaled = scaler_X.fit_transform(X)
y_scaled = scaler_y.fit_transform(y.reshape(-1, 1))

# -------------------------------------------------
# 3. เทรน MLP
# -------------------------------------------------
mlp_model = MLPRegressor(hidden_layer_sizes=(64,64,32),
                         activation='relu',
                         solver='adam',
                         max_iter=500,
                         random_state=42)

mlp_model.fit(X_scaled, y_scaled.ravel())

# -------------------------------------------------
# 4. เซฟไฟล์โมเดลและ Scaler
# -------------------------------------------------
with open("models/mlp_model.pkl", "wb") as f:
    pickle.dump(mlp_model, f)

with open("models/scaler_X.save", "wb") as f:
    pickle.dump(scaler_X, f)

with open("models/scaler_y.save", "wb") as f:
    pickle.dump(scaler_y, f)

print("✅ MLP model and scalers saved to 'models/'")
